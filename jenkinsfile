pipeline {
    agent any
    tools {
        maven 'Maven'
    }
    stages('Build') {
        steps{
            sh 'mvn clean install'
        }
    }stages('Test'){
        steps {
            sh 'mvn test'
        }

    }stages('Deploy'){
        steps{
            echo 'Deploying the application'
        }
    }
}